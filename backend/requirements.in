# Direct production dependencies. Regenerate requirements.txt with
# ../scripts/compile_requirements.sh after changing this file.
cachetools==7.1.4
ccxt==4.5.67
pandas==3.0.3
litestar==2.24.0
msgspec==0.21.1
python-socketio==5.16.3
websockets==16.1.1
Telethon==1.44.0
ta-lib==0.7.1
tenacity==9.1.4
tortoise-orm==1.1.7
anyio==4.14.2
sniffio==1.3.1
aiosqlite==0.22.1
redis==8.0.1
httpx==0.28.1
uvicorn==0.51.0

# Security floors for transitives in the HTTP/exchange stack. Keeping them as
# direct inputs prevents a future resolver run from reintroducing known CVEs.
aiohttp==3.14.1
click==8.4.2
cryptography==49.0.0
idna==3.18
# CCXT 4.5.67 hard-pins this release. PYSEC-2026-3447 affects creating source
# distributions on Unicode-normalizing macOS filesystems, not runtime use.
# Moonwalker installs wheels only, so the vulnerable sdist code is unreachable.
setuptools==82.0.1
urllib3==2.7.0
